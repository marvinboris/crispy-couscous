export default function Logo({ className = '' }) {
    return <img className={`h-[112.95px] w-auto ${className}`} src="/images/logo.svg" alt="Logo" />
    return <span className="text-primary-600 font-bold text-3xl">PSR.</span>
}